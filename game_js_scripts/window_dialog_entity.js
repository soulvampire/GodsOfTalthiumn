game = game || {};

/*****************/
/* dialog object */
/*****************/
game.window_dialog_object = Entity.extend(
{
  init: function (x, y)  
  {
    settings = {};
    settings.image = background;
  }
});